[project]
name = "job-filter-core"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "pydantic",
  "python-jobspy",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/job_filter_core"]
